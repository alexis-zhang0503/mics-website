# AGENTS.md

## 网站定位

本仓库是南京邮电大学计算机学院“物联网感知小组”的小型静态官方网站。当前负责人为左益平讲师。第一版为单页展示网站，不建设成员管理系统。

## 技术栈

- HTML5
- CSS3
- 原生 JavaScript
- GitHub Pages

不得无必要引入 React、Vue、Angular、构建工具、npm 依赖、后端、数据库、API 或 CMS。

## 网站结构

主要页面为 `index.html`，依次包含：Hero、课题组介绍、负责人介绍、研究方向、学术成果、本科生招新、联系方式和 Footer。

样式位于 `css/style.css`，交互位于 `js/main.js`，图片位于 `assets/images/`。

## 内容规则

1. 禁止自行虚构课题组、教师、研究方向、论文、项目、招新要求、联系方式或任何其他真实信息。
2. 用户未提供的信息必须保持为“待补充”“TODO: 后续补充”或在不影响结构时隐藏。
3. 不得用听起来合理的通用研究描述替换占位内容。
4. 新增真实内容时，只使用用户明确提供或明确授权采用的信息。

## 维护规则

1. 优先保持纯静态网站，确保直接打开 `index.html` 时主要内容可用。
2. 修改时保持当前简洁、现代、学术、工程技术感的视觉风格。
3. 颜色优先通过 `:root` 中的 CSS Variables 调整，避免散落新的颜色值。
4. 新增内容优先复用和修改已有结构，不随意重构或扩展目录。
5. 不增加无关页面、复杂动画、后台功能或管理系统。
6. 保持语义化 HTML、键盘可访问性、移动端导航和响应式布局。
7. 所有站内资源使用相对路径，禁止本地绝对路径、localhost 或运行时环境变量依赖。
8. GitHub Pages 必须始终保持可部署；保留 `.nojekyll`。
9. 每次修改后检查导航锚点、控制台错误、横向溢出和移动端显示。
10. 更新结构或维护方式时，同步更新 `README.md`。
